<Window x:Class="RemoteSupport.Agent.App.ConsentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RemoteSupport.Agent.App"
        Title="{DynamicResource ConsentTitle}" Width="520" Height="560"
        MinWidth="420" MinHeight="460" WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="24" FontWeight="SemiBold" Text="{DynamicResource ConsentTitle}"
                   AutomationProperties.Name="{DynamicResource ConsentRequestName}" />
        <StackPanel Grid.Row="1" Margin="0,20,0,0">
            <TextBlock Text="{DynamicResource VerifiedOperator}" FontWeight="SemiBold" />
            <TextBlock Text="{Binding OperatorDisplayName}" FontSize="18"
                       AutomationProperties.Name="{DynamicResource VerifiedOperatorName}" />
            <TextBlock Text="{Binding TenantDisplayName}" Margin="0,4,0,0"
                       AutomationProperties.Name="{DynamicResource VerifiedOrganizationName}" />
            <TextBlock x:Name="VerifiedStateText" Margin="0,4,0,0" FontWeight="SemiBold"
                       AutomationProperties.Name="{DynamicResource VerificationStateName}" />
        </StackPanel>
        <TextBlock Grid.Row="2" Margin="0,20,0,8" Text="{DynamicResource RequestedPermissions}" FontWeight="SemiBold" />
        <ListBox Grid.Row="3" ItemsSource="{Binding ScopeChoices}" AutomationProperties.Name="{DynamicResource RequestedPermissionsName}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Padding="4" AutomationProperties.Name="{Binding Scope}">
                        <TextBlock Text="{Binding Scope, Converter={x:Static local:ScopeLabelConverter.Instance}}" TextWrapping="Wrap" />
                    </CheckBox>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Row="4" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="ExpiryStateText" VerticalAlignment="Center"
                       AutomationProperties.Name="{DynamicResource RequestExpiryName}" />
            <Button Grid.Column="1" MinWidth="100" Margin="8,0" Padding="14,8" Content="{DynamicResource Deny}"
                    IsCancel="True" Click="DenyClicked" AutomationProperties.Name="{DynamicResource DenyConsentName}" />
            <Button Grid.Column="2" MinWidth="100" Padding="14,8" Content="{DynamicResource Approve}"
                    IsDefault="True" Click="ApproveClicked" AutomationProperties.Name="{DynamicResource ApproveConsentName}" />
        </Grid>
    </Grid>
</Window>
