<Window x:Class="RemoteSupport.LocalViewer.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RemoteSupport.LocalViewer.App"
        Title="Remote Support Local Viewer" Width="1100" Height="720">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8">
      <TextBlock Text="Source" VerticalAlignment="Center" Margin="0,0,6,0" />
      <ComboBox x:Name="SourceSelector" Width="120" SelectedIndex="0">
        <ComboBoxItem Content="DXGI" />
        <ComboBoxItem Content="WGC" />
        <ComboBoxItem Content="Synthetic" />
      </ComboBox>
      <TextBlock Text="Display" VerticalAlignment="Center" Margin="12,0,6,0" />
      <ComboBox x:Name="DisplaySelector" Width="420" DisplayMemberPath="Name" />
      <Button Content="Start" Click="Start_Click" Margin="12,0,4,0" Padding="16,4" />
      <Button Content="Stop" Click="Stop_Click" Padding="16,4" />
    </StackPanel>
    <Border Grid.Row="1" Background="Black" Margin="8">
      <local:NativeRenderHost x:Name="RenderHost" />
    </Border>
    <TextBlock x:Name="StatusText" Grid.Row="2" Margin="8" Text="Initializing native runtime…" />
  </Grid>
</Window>

