# Build Artifacts

The build creates ignored output below this directory: binaries, packages, test results, coverage, generated contract evidence, dependency inventories, SBOMs, and release evidence. No signing material or private symbol bundle is committed.

